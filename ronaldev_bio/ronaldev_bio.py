import reflex as rx
import ronaldev_bio.constants as const
import ronaldev_bio.styles.styles as styles
from ronaldev_bio.pages.index import index
from ronaldev_bio.pages.proyects import proyects

app = rx.App(
    stylesheets=styles.STYLESHEETS,
    style = styles.BASE_STYLE,
    
)
